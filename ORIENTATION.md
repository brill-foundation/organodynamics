# Orientation

*The living standpoint for any participant arriving — the desk. Read after
`CLAUDE.md`, before looking through the Window. Transient by design: it holds only
what is true **now** and points to everything durable. It restates nothing a
handoff or the architecture already says; where it seems to, the pointed‑at
document is right.*

## Current standpoint

- **Reality:** a follow‑up workshop after `Organodynamics 006`. The two Living‑Map
  gating decisions are closed (containment spine · zoom depth), and the Arrival /
  Orientation startup convention has just been established.
- **Repository:** `brill-foundation/organodynamics`
- **Active branch:** `claude/brill-architecture-gating-v5obzm` (based on the
  `…-stewardship-onboard-ei4jwx` tip; check `git log -1` for the live HEAD).
- **Preview:** Vercel Preview is the review surface; its live `vercel.app` URL is
  egress‑blocked from the agent — render the deployed commit locally
  (`python3 -m http.server` + headless Chromium) as the substitute.
- **Production (`main`):** untouched; ~58 commits behind this branch. Releasing to
  `main` is an explicit human decision (PR #2 tracks the `ei4jwx → main` release).
- **Current priority:** the **paper geography** — write Brill's canonical `within()`
  structure down (BRILL_EXPERIENCE_ARCHITECTURE §7 step 2), still no Map code.

## Where to look next (the switchboard)

- **Discipline / constitutional work** → `docs/HANDOFF.md` (stewardship continuity)
- **Brill / product work** → `docs/BRILL_HANDOFF.md`, then
  `docs/BRILL_EXPERIENCE_ARCHITECTURE.md` (§5 decisions · §7 starting point)
- **The corpus itself** (Constitution · RFCs · Cabinet) → sovereign; read per that
  layer's handoff, never modified by Brill work
- **A human first‑timer** → `README.md` → `laboratory/arrival/WELCOME.md`
- **The distributable engineering kit** → `blueprint/package/START_HERE.md`

## The rule of this document

Only the standpoint above is owned here — the live, global state that belongs to no
single layer. Everything else is a pointer. When the branch, priority, or reality
changes, this header changes; the handoffs and the architecture doc evolve on their
own. Keep it short; keep it true; restate nothing.
